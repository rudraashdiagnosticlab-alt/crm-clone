/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the Next.js dev tools indicator (the floating Route/Bundler/Preferences menu).
  devIndicators: false,
};

export default nextConfig;
